services:
  db:
    image: postgres:15
    container_name: time-tracker-db
    environment:
      POSTGRES_DB: tracker_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"